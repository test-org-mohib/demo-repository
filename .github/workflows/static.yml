# Unit Test reusable workflow
name: Unit Test

on:
  workflow_call:
    inputs:
      target-branch:
        required: true
        type: string
  pull_request:
    branches:
      - main
        
  
jobs:
  # Single deploy job since we're just deploying
  execut_unit_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Execute
        run : |
            echo 'TODO Execute Tests'
