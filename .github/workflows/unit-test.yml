# Unit Test reusable workflow
name: Unit Test

on:
  # pull_request:
  #   branches: ["main"]
  workflow_call:

jobs:
  # Single deploy job since we're just deploying
  execut_unit_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Execute
        run: exit 1
